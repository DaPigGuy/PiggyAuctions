database:
  # Options: sqlite, mysql
  type: mysql
  sqlite:
    file: "auctions.sqlite"
  mysql:
    host: localhost
    username: root
    password: password
    schema: "piggyauctions"
  worker-limt: 1
economy:
  # Options: economyapi, multieconomy, xp
  provider: economyapi